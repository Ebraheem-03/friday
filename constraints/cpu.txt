# CPU-only install constraints — PROJECT RULE (Ebraheem): never install CUDA builds.
#
# kokoro (the `voice` extra) pulls in torch, which on Linux defaults to a large
# CUDA wheel from PyPI. We force the CPU-only wheels from the official PyTorch
# CPU index instead. pip honours --extra-index-url lines inside this file.
#
# Usage (echo / Step 4 and any local voice work):
#   pip install '.[voice]' -c constraints/cpu.txt
#
# echo (Step 4) must pin the exact torch (+cpu) / transformers versions kokoro
# 0.9.4 resolves to and add them below with `==` so the CPU build is locked.

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.12.1+cpu           # pinned by echo (Step 4); from PyTorch CPU index
transformers==5.12.1        # pinned by echo (Step 4); resolved by kokoro==0.9.4
spacy==3.8.14               # pinned by echo (Step 4); English G2P for misaki/kokoro — CPU-only pipeline
ctranslate2==4.8.0          # pinned by echo (Step 9a); faster-whisper inference engine — CPU-only wheels only (zero nvidia/CUDA packages confirmed 2026-06-23)

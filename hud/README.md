# FRIDAY HUD (Electron + React + Three.js)

Owned by `halo`. Borderless translucent telemetry HUD with audio-reactive visuals and circular ring controllers, driven by the runtime state machine over a telemetry WebSocket (CPU/RAM + audio frequency).

Scaffold in Step 8. Optional design accel: frontend-design skill, Stitch (OAuth), 21st.dev. No browser storage.
